services:
  nginx:
    image: nginx:latest
    container_name: site-teste
    restart: unless-stopped

    ports:
      - "8081:80"

    volumes:
      - ./html:/usr/share/nginx/html:ro

    networks:
      - proxy

networks:
  proxy:
    external: true
